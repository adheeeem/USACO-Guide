#include <bits/stdc++.h>

#define ll long long

using namespace std;

void solve()
{
    ll n, arr[101];
    cin >> n;
    for (int i = 0; i < n; i++)
    {
        cin >> arr[i];
    }
    sort(arr, arr + n);

}

int calc(int start, int arr[], int n) {
    int l = start - 1, r = start + 1;
    for (int i = 0; i < n; i++) {
        
    }
}

int main()
{
    ll t;
    // cin >> t;
    t = 1;
    while (t--)
        solve();
}